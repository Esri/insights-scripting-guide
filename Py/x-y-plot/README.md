## Python Matplotlib chart

This is a beginner sample.  Using the matplotlib, the sample plots values to create a line graph.  It also creates a label on the y-axis.  Data values are hard coded in this script.